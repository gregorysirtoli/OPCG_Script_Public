env:
  PYTHONUNBUFFERED: "1"

  # Caricamento provider (modulo della repo privata clonata nel job)
  PROVIDERS_MODULE: private_providers.bundle

  # Abilito SEMPRE il secondario
  SECONDARY_ENABLED: "1"
  SECONDARY_FORCE: "1"

  # Tuning non sensibile
  PRICES_BATCH: "500"
  MONGO_PAGE_SIZE: "200"
  RATE_PER_SEC: "1.2"
  BURST: "10"
  LOG_LEVEL: "DEBUG"
  SAMPLE_LIMIT: "200"
  DISABLE_SHARDING: "true"
  MAIL_SUBJECT: "[Ingestor] Report"

  # Facoltativo fallback cambio (se NON usi FX_API_URL)
  FX_FIXED_RATE: "1.15"

  # Secrets 
  MONGODB_URI: ${{ secrets.MONGODB_URI }}
  MONGODB_DB:  ${{ secrets.MONGODB_DB }}
  EMAIL_ADDRESS: ${{ secrets.EMAIL_ADDRESS }}
  EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
  EMAIL_TO: ${{ secrets.EMAIL_TO }}
  MAIL_FROM: ${{ secrets.MAIL_FROM }}
  FX_API_URL: ${{ secrets.FX_API_URL }}
  SECONDARY_LABEL_MAP: ${{ secrets.SECONDARY_LABEL_MAP }}
  SECONDARY_SELECTORS: ${{ secrets.SECONDARY_SELECTORS }}
  CM_PRICEGUIDE_URL: ${{ secrets.CM_PRICEGUIDE_URL }}
